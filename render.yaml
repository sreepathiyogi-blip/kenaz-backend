services:
  - type: web
    name: kenaz-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: PERPLEXITY_API_KEY
        sync: false
